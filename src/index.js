import React from 'react';
import ReactDOM from 'react-dom';
import Header from './components/header/header';

const wrapper = document.getElementById("main");
ReactDOM.render(<Header title="Example"/>, wrapper);